apiVersion: v1
kind: Service
metadata:
  name: {{ include "supersense.fullname" . }}
  labels:
    {{- include "supersense.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "supersense.selectorLabels" . | nindent 4 }}
